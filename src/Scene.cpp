
// Written By: Roman Larionov
// Copyright: Roman Larionov 2014

#include "Scene.h"

namespace vv
{
    namespace scene
    {
        Scene::Scene()
        {
        }

        Scene::~Scene()
        {
        }
        /*
        void Scene::addObjects(vv::Mesh::SimpleMeshObject object)
        {
           this->objects.push_back(object);
        }
        */
        /*
        void Scene::draw(auto uniformSetter, auto attributeSetter)
        {
            for (auto currObject = objects->begin(); currObject != objects->end(); currObject++)
                currObject->draw(uniformSetter, attributeSetter);
        }
        */
    }
}



